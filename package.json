{
  "name": "oxygen11",
  "version": "1.3.0",
  "description": "منظومة مُتقن أكسجين للصيانة والمقاولات",
  "main": "server.js",
  "private": true,
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check database/db.js && node -e \"require('./database/db')\"",
    "test": "node --test",
    "ci": "npm run check && npm test"
  },
  "dependencies": {
    "cookie-parser": "^1.4.7",
    "express": "^4.19.2",
    "firebase-admin": "^13.5.0",
    "socket.io": "^4.7.5",
    "mysql2": "^3.14.4"
  },
  "engines": {
    "node": ">=20"
  }
}
